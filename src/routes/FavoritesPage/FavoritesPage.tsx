import { FC } from 'react';
import { TSearchResult } from '../../types';

export const FavoritesPage: FC = () => {
  return (
    <div>
      Favoriten{' '}
      {/* {items.map((item) => (
        <div>{item.title}</div>
      ))} */}
    </div>
  );
};
